// Wrapper entry for Render when Start command points to backend/two-video-compare.ts
// It simply imports the real entry at the repo root so relative imports work.
import '../two-video-compare.ts';

